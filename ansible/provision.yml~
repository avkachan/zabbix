- name: Provision with Ansible
  hosts: all
  become: yes

  vars:
     zabbix:
      - zabbix
      - zabbix-server
      - zabbix-server-mysql
      - zabbix-web-mysql
      - zabbix-agent

  roles:
  - zabbix 
  

  post_tasks:
  - debug: var=services_info.split('\n')
